# 4.28
# 大O分析
- **大O符号（Big O notation）**：是用于描述函数渐进行为的数学符号。
- 循环次数，实际上就是我们需要知道的大致执行次数

| 函数         | 类型    | 解释                                         | 例子                   |
| ---------- | ----- | ------------------------------------------ | -------------------- |
| O(1)       | 常数阶   | 如果算法能够优化到这个程度，那么基本上算是最快的算法了。               | 数组索引查找、哈希表查找         |
| O(log⁡2n)  | 对数阶   | 仅次于常数阶的速度                                  | 二分查找、完全二叉树操作         |
| O(n)       | 线性阶   | 线性表插入、删除数据，包括动态规划类算法能够达到线性阶。               | 遍历数组、求和              |
| O(nlog⁡2n) | 线性对数阶 | 相当于在对数阶算法外层套了一层线性阶循环。                      | 快速排序、归并排序、堆排序        |
| O(n2))     | 平方阶   | 我们前面学习的冒泡排序，需要进行两重循环，实际上就是平方阶。             | 冒泡排序、选择排序            |
| O(n3)      | 立方阶   | 从立方阶开始，时间复杂度就开始变得有点大了。                     | Floyd-Warshall（最短路径） |
| O(2n)      | 指数阶   | 我们前面介绍的斐波那契数列递归算法，就是一个指数阶的算法，因为它包含大量的重复计算。 | 递归解决子集问题、斐波那契数列      |
| O(n!)      | 阶乘    | 这个增长速度比指数阶还恐怖，但是一般很少有算法能达到这个等级。            | 全排列问题、TSP 问题（暴力解法）   |

# 线性表
一种是顺序存储实现，还有一种是链式存储实现
## 顺序表
## 链表
## 栈
栈底：先入
栈顶：先出
### 共享栈
将一个固定长度的数组共享给两个栈来使用
数组的两头分别作为两个栈的栈底，当两个栈的栈顶指针相遇时（栈顶指针下标之差绝对值为1时），表示栈已满。
### 用链表实现栈
## 队列
一般采用循环队列的形式，来实现重复使用一个数组
由于队首指针和队尾指针重合时表示队列为空，所以我们只能舍弃一个存储单元，当队尾距离队首一个单元的时候，表示队列已满。
### 用链表实现队列
# 树形结构
## 树
分支只能向后单独延伸，**不能与其他分支上的结点相交**
- 位于最上方的结点为树的**根结点**（Root）。
- 每个结点连接的子结点数目（分支的数目），我们称为结点的**度**（Degree），而各个结点度的最大值称为**树的度**。
- 每个结点延伸下去的下一个结点都可以称为一棵**子树**（SubTree）。
- 每个**结点的层次**（Level）按照从上往下的顺序，树的根结点为`1`，每向下一层`+1`。整棵树中所有结点的最大层次，就是这颗**树的深度**（Depth）。
- 与当前结点直接向下相连的结点，我们称为**子结点**（Child）
- 如果某个节点没有任何的子结点（结点度为0时）那么我们称这个结点为**叶子结点**
- 如果两个结点的父结点是同一个，那么称这两个节点为**兄弟结点**（Sibling）
- 从根结点开始一直到某个结点的整条路径的所有结点，都是这个结点的**祖先结点**（Ancestor）
### 森林
m（m≥0）棵树的集合我们称为**森林**（Forest）
## 二叉树
Binary Tree
它的度最大只能为`2`
二叉树任何结点的子树是有左右之分的，不能颠倒顺序
### 满二叉树
在一棵二叉树中，所有分支结点都存在左子树和右子树，且叶子结点都在同一层：

![image-20220801231216578](https://oss.itbaima.cn/internal/markdown/2022/08/01/btfjlJhDuWrSXYi.png)

这样的二叉树我们称为**满二叉树**

还有一种特殊情况：

![image-20220801224008266](https://oss.itbaima.cn/internal/markdown/2022/08/01/QGLfnYWFby37deP.png)

可以看到只有最后一层有空缺，并且所有的叶子结点是按照从左往右的顺序排列的，这样的二叉树我们一般称其为**完全二叉树**，所以，一棵满二叉树，一定是一棵完全二叉树。
### 与树和森林的转换
二叉树和树、森林之间是可以相互转换的。
#### 树和二叉树的转换
![image-20220806101322807](https://oss.itbaima.cn/internal/markdown/2022/08/06/y51pTzhrQV3GPCJ.png)
![image-20220806102932517](https://oss.itbaima.cn/internal/markdown/2022/08/06/6JxYP2CXSyZdGa4.png)
兄弟节点为右子树，子节点为左子树
转换为二叉树后，**根节点一定没有右子树**。

#### 森林和二叉树的转换
![image-20220808113135783](https://oss.itbaima.cn/internal/markdown/2022/08/08/QCIaYTcEv2NO47G.png)
![image-20220808113251636](https://oss.itbaima.cn/internal/markdown/2022/08/08/O3xnhv85WLPzJpq.png)
根节点存在右子树
右子树连接的都是森林中其他的树。

### 二叉树的性质
- **性质一：** 对于一棵二叉树，第`i`层的最大结点数量为 $2^{i−1}$ 个，比如二叉树的第一层只有 1 个根结点，而二叉树的第三层可以有 4 个结点。
- **性质二：** 对于一棵深度为`k`的二叉树，可以具有的最大结点数量为$$n=2^0+2^1+2^2+...+2^{k−1}$$我们发现，实际上每一层的结点数量，组成了一个等比数列，公比`q`为`2`，结合等比数列求和公式，我们可以将其简化为：$$n=2^k−1$$
- **性质三：** 假设一棵二叉树中度为0、1、2的结点数量分别为$n_0​、n_1​、n_2$​，由于一棵二叉树中只有这三种类型的结点，那么可以直接得到结点总数：$$n=n_0+n_1+n_2​$$边数之和$E = n_1+2n_2 = n - 1$得，$$n_0 = n_2+1$$
- **性质四：** 一棵完全二叉树结点数`n`满足：$$2^{k-1}-1<n\leq 2^k-1$$由于n为正整数，$$2^{k-1}\leq n\leq 2^k-1$$对左边取对数，化解得一棵具有`n`个结点的完全二叉树深度为$$k = \lfloor\log_{2}n\rfloor  +1$$
- **性质五：** 一颗有`n`个结点的完全二叉树，由性质四得到深度为 k=⌊log2n⌋+1k=⌊log2​n⌋+1 现在对于任意一个结点`i`，结点的顺序为从上往下，从左往右：
    - 对于一个拥有左右孩子的结点来说，其左孩子为`2i`，右孩子为`2i + 1`。
    - 如果`i = 1`，那么此结点为二叉树的根结点，如果`i > 1`，那么其父结点就是 ⌊i/2⌋，比如第3个结点的父结点为第1个节点，也就是根结点。
    - 如果`2i > n`，则结点`i`没有左孩子，比如下面图中的二叉树，n为5，假设此时`i = 3`，那么`2i = 6 > n = 5` 说明第三个结点没有左子树。
    - 如果`2i + 1 > n`，则结点`i`没有右孩子。
    - ![image-20220805231744693](https://oss.itbaima.cn/internal/markdown/2022/08/05/uan6A3ZRLykt289.png)
**完全二叉树最下层的叶子节点必须严格按照“从左向右”紧密排列，中间不能有空隙。**

**n个结点可以构造出多少种不同的二叉树**
动态规划
![[Pasted image 20260506134112.png]]
### 二叉树的构建
二叉树的存储形式也可以使用我们前面的两种方式，一种是使用数组进行存放，还有一种就是使用链式结构
### 二叉树的遍历
一共有四种访问方式：**前序遍历、中序遍历、后序遍历、层序遍历。**
#### 前序遍历
先走左边再走右边
递归或栈
![image-20220806171431845](https://oss.itbaima.cn/internal/markdown/2022/08/06/qCFMosHtujEZ3U6.png)
#### 中序遍历
需要先完成整个左子树的遍历后再打印，然后再遍历其右子树。
![image-20220806230603967](https://oss.itbaima.cn/internal/markdown/2022/08/06/W6Yb5M92gQApNJa.png)
A的左子树中所有结点，中序遍历结果中全部都在A的左边，右子树中所有的结点，全部都在A的右边
#### 后序遍历
继续将打印的时机延后，需要等待左右子树全部遍历完成，才会去进行打印。
![image-20220806233407910](https://oss.itbaima.cn/internal/markdown/2022/08/06/YE2rODdqpCInUa9.png)
整棵二叉树（包括子树）根结点一定是在后面的
使用栈时，需要稍微对结点的结构进行修改，添加一个标记变量，来表示已经完成左边还是左右都完成
#### 层序遍历
![image-20220807205135936](https://oss.itbaima.cn/internal/markdown/2022/08/07/ywF6r9MU1JSPIge.png)
利用队列来实现层序遍历

## 高级树结构
### 线索化二叉树
线索化的规则为：
- 结点的左指针，指向其当前遍历顺序的前驱结点。
- 结点的右指针，指向其当前遍历顺序的后继结点。
还需要分别为左右添加一个标志位，来表示左右指针到底指向的是孩子还是遍历线索

要完成后续遍历，我们只能对结点进行改造

### 二叉查找树
也叫二叉搜索树或是二叉排序树
- 左子树中所有结点的值，均小于其根结点的值。
- 右子树中所有结点的值，均大于其根结点的值。
- 二叉搜索树的子树也是二叉搜索树。
二叉查找树不能插入重复元素，如果出现重复直接忽略
二叉查找树的删除操作：
1. 要删除的结点是叶子结点。
2. 要删除的结点是只有一个孩子结点。
3. 要删除的结点有两个孩子结点。
	1. 选取其左子树中最大结点上位
	2. 选择其右子树中最小结点上位

### 平衡二叉树
- 平衡二叉树一定是一棵二叉查找树。
- 任意结点的左右子树也是一棵平衡二叉树。
- 从根节点开始，左右子树都高度差不能超过1，否则视为不平衡。
![image-20220815115836604](https://oss.itbaima.cn/internal/markdown/2022/08/15/KcOQVhlFxzwsIb9.png)
**LL型调整**（右旋）
**RR型调整**（左旋）
**RL型调整**（先右旋，再左旋）
**LR型调整**（先左旋，再右旋）

### 红黑树
红黑树也是二叉查找树的一种
- 规则1：每个结点可以是黑色或是红色。
- 规则2：根结点一定是黑色。
- 规则3：红色结点的父结点和子结点不能为红色，也就是说不能有两个连续的红色。
- 规则4：所有的空结点都是黑色（空结点视为NIL，红黑树中是将空节点视为叶子结点）
- 规则5：每个结点到空节点（NIL）路径上出现的黑色结点的个数都相等。
所有新插入的结点，默认情况下都是红色
**颜色变换**
将父结点和其兄弟结点同时修改为黑色（为啥兄弟结点也需要变成黑色？因为要满足性质5）然后将爷爷结点改成红色，再保证根节点为黑色
对于**父结点为红色，父结点的兄弟结点为黑色**（NIL视为黑色）的情况，变色无法解决问题了，那么我们只能考虑旋转了

## 其他树结构
### B树和B+树
是专门为磁盘数据读取设计的一种度为 m 的查找树（多用于数据库）
是平衡树，不是二叉树
每个结点现在可以保存多个值，每个结点可以连接多个子树

在B树中，**所有的节点都同时存储键（Key）和具体的数据（Value）**。
在 B+树中，非叶子节点**只存储索引（Key），不存储具体数据（Value）**。只有到了最底层的叶子节点，才存储完整的数据。
**数据库偏爱 B+树**

**红黑树** 和 **4阶B树**（2-3-4树）具有等价性，其中黑色结点就是中间的（黑色结点一定是父结点)，红色结点分别位于两边，通过将黑色结点与它的红色子节点融合在一起，形成1个B树节点
### 哈夫曼树
> 给定N个权值作为N个叶子结点，构造一棵二叉树，若该树的带权路径长度达到最小，称这样的二叉树为最优二叉树，也称为哈夫曼树（Huffman Tree）

哈夫曼树的一个比较重要应用就是对数据进行压缩，它是现代压缩算法的基础
### 堆和优先级队列
必须是一棵完全二叉树，树中父亲都比孩子小的我们称为**小根堆**（小顶堆），树中父亲都比孩子大则是**大根堆**
一般堆都是以数组形式存放
**“上浮”用于向堆中【新增】元素，“下潜”用于从堆中【删除】元素（或建堆）。**


# 散列表
**散列**（也可以叫哈希 Hash）
采用直接寻址的方式，在理想情况下，查找的时间复杂度可以达到常数阶 O(1)。
## 哈希函数
应用最为广泛的是SHA-1和MD5
## 散列表
哈希表长度是多少（长度最好是一个素数），模就是多少
## 哈希冲突
### 线性探测法
开放定址法包含，线性探测法、平方探测法、双散列法等
### 链地址法
常见的哈希冲突解决方案是**链地址法**，当出现哈希冲突时，我们依然将其保存在对应的位置上，我们可以将其连接为一个链表的形式

# 图结构
图（Graph）一般由两个集合共同构成，一个是非空但是有限的顶点集合V（Vertex），另一个是描述顶点之间连接关系的边集合E（Edge，边集合可以为空集）
图有两种基本形式，有向图和无相图
有向图的边使用尖括号<>表示。
如果是无向图的一条边(A,B)，那么就称A、B互为邻接点；如果是有向图的一条边<A,B>，那么就称起点A邻接到终点B。
有向图的每个结点分为入度和出度，其中入度就是与顶点相连且指向该顶点的边的个数，出度就是从该顶点指向邻接顶点的边的个数。
只要我们的图中不出现自回路边或是重边，那么我们就可以称这个图为简单图
如果在一个无向图中，任意两个顶点都有一条边相连，则称该图为**无向完全图**
同样的，在一个有向图中，如果任意两顶点之间都有由方向互为相反的两条边连接，则称该图为**有向完全图**
如果图中任意两点都是连通的，那么我们就称这个图为**连通图**。对于有向图，如果图中任意顶点A和B，既有从A到B的路径，也有B到A的路径，则称该有向图是**强连通图**
对于图 G=(V,E) 和 G′=(V′,E′)，若满足 V′是 V 的子集，并且 E′ 是 E 的子集，则称 G′ 是 G 的**子图**
无向图的极大连通子图称为**连通分量**，有向图的极大连通子图称为**强连通分量**。
	无向图的连通分量，其实就是把一个大图，撕裂成几个互相不挨着的“独立小团体”。
## 存储结构
### 邻接矩阵
对于一个不带权值的图来说：
![[Pasted image 20260508120617.png]]
对于一个带权值的图来说，如果有边，则直接填写对应边的权值，如果没有，那么就填写0或是∞
![[Pasted image 20260508120647.png]]

无向图的邻接矩阵一定是**沿对角线对称**的。

- 无向图的邻接矩阵一定是一个对称矩阵，因此，有时为了节省时间，我们可以只存放上半部分。
- 对于无向图，邻接矩阵的第`i`行非0（或非∞）的个数就是第`i`个顶点的度。
- 对于有向图，邻接矩阵的第`i`行非0（或非∞）的个数就是第`i`个顶点的出度（纵向就是入度了）

**优点**
- **查路极快：**
- **极其简单**
**缺点**
1. **极其浪费空间**
2. **找邻居慢**

- **点少、边多（稠密图），或者需要疯狂查询某两点之间连通性时** -> 选**邻接矩阵**。
- **点多、边少（稀疏图，如社交网络、互联网地图）** -> 不要用它，去用**邻接表**（字典/哈希表）。

### 邻接表
使用链式结构存储
对于图中的每个顶点，建立一个数组，存放一个头结点，我们将与其邻接的顶点，通过一个链表进行记录
## 图的遍历
### 深度优先搜索（DFS）
```
void dfs(参数) {
    if (终止条件) {
        存放结果;
        return;
    }
    for (选择：本节点所连接的其他节点) {
        处理节点;
        dfs(图，选择的节点); // 递归
        回溯，撤销处理结果
    }
}
```
### 广度优先搜索（BFS）
## 图应用
### 生成树和最小生成树
**极小连通子图**：是原图的子图并且是连通的，但是此时要求具有最大的顶点数和最小的边数，也就是说再去掉任意一条边会导致图不连通（理解为极大连通子图尽可能去掉能去掉的边）
极小连通图不唯一

我们发现，无论是去掉哪些边的情况，到最后一定是只留下 N-1 条边（其中N是顶点数）每个顶点有且仅有一条路径相连，也就是包含原图全部N个顶点的极小连通子图，我们一般称其为：**生成树**
如果原图本身不连通的话，那么就会出现多个连通分量，此时就会得到一片**生成森林**，森林中的树的数量就是其连通分量的数量。

如果我们给一个无向图的边都加上权值（网图）现在要求生成树边的权值总和最小，我们就称这棵树为**最小生成树**
最小生成树不唯一
**普利姆（Prim）算法**
普利姆算法的核心就是从任意一个顶点开始，不断成长为一棵树，每次都会选择尽可能小的方向去进行延伸
**克鲁斯卡尔（Kruskal）算法**
核心思想就是我们主动去选择那些小的边
### 最短路径问题
**单源最短路径**
一个顶点出发，到其他顶点的最短路径
**迪杰斯特拉（Dijkstra）算法**

**图中每一对顶点之间的最短路径**
**弗洛伊德（Floyd）算法**
根据最初的邻接矩阵进行推导
- 每一轮，我们都会去更新那些非对角线（对角线都是0，更新了还是0，所以说没必要看）、`i`行`i`列以外的元素，判断水平和垂直方向投影的两个元素之和是否比原值小，如果是，那就更新为新的值。迭代公式为：$$A_k(i,j)=min(A_{k−1}(i,j),A_{k−1}(i,k)+A_{k−1}(k,j))$$
- 经历n轮后，最后得到的就是最终的最短距离了。
### 拓扑排序
**有向无环图**（DAG）
AOV图
**拓扑排序**（Topological Order）是指，将一个有向无环图（Directed Acyclic Graph）进行排序进而得到一个有序的线性序列。
拓扑排序不唯一
将那些入度为0的顶点，丢进队列中，然后删除节点
### 关键路径计算
AOE图
**整个项目的最短时间取决于最长的那条路径**。这条路径就是**关键路径**。
**ve (Earliest Time) - 最早开始时间**
**vl (Latest Time) - 最晚开始时间**
如果一个任务的 **最早开始时间 == 最晚开始时间 (ve == vl)**，说明这个任务**一点松懈的余地都没有**。它就是**关键活动**，由它们串起来的路线就是**关键路径**。

# 排序算法

**对于序列中的相同元素，如果排序之后它们的相对位置没有发生改变，则称该排序算法为「稳定排序」，反之则为「不稳定排序」**。

## 基础排序
### 冒泡排序 
- 每轮排序都会将剩余元素中最大的一个推到最右边，下次排序就不再考虑这些已经在对应位置的元素。
- **最好情况时间复杂度：** O(n)，如果本身就是有序的，那么我们只需要一次遍历，当标记检测到没有发生交换，直接就结束了，所以说一遍就搞定。
- **空间复杂度：** 因为只需要一个变量来暂存一下需要交换的变量，所以说空间复杂度为 O(1)O(1)
- **最坏情况时间复杂度：** O(n2)，也就是硬生生把每一轮都吃满了，比如完全倒序的数组就会这样。
- **稳定性：** 稳定
### 插入排序
- 每轮排序会从后面依次选择一个元素，与前面已经处于有序的元素，从后往前进行比较，直到遇到一个不大于当前元素的的元素，将当前元素插入到此元素的前面。
- 插入元素后，后续元素则全部后移一位。
- **最好情况时间复杂度：** O(n)，如果本身就是有序的，因为插入的位置也是同样的位置，当数组本身就是有序的情况下时，每一轮我们不需要变动任何其他元素。
- **最坏情况时间复杂度：** O(n2)，比如完全倒序的数组就会这样，每一轮都得完完整整找到最前面插入。
- **空间复杂度**：同样只需一个变量来存一下抽出来的元素，所以说空间复杂度为 O(1)O(1)
- **稳定性：** 稳定
### 选择排序
- 每轮排序会从后面的所有元素中寻找一个最小的元素出来，然后与已经排序好的下一个位置进行交换。
- 是**不稳定的**排序算法
- - **最好情况时间复杂度：** O(n2)，即使数组本身就是有序的，每一轮还是得将剩余部分挨个找完之后才能确定最小的元素，所以说依然需要平方阶。
- **最坏情况时间复杂度：** O(n2)
- **空间复杂度**：每一轮只需要记录最小的元素位置即可，所以说空间复杂度为 O(1)O(1)
- **稳定性：** 不稳定
## 进阶排序
### 快速排序
**快速排序**是冒泡排序的进阶版本
快速排序每一轮的目的就是将大的丢到基准右边去，小的丢到基准左边去。
快速排序算法，是一种**不稳定的**排序算法
- 在一开始，排序范围是整个数组
- 排序之前，我们选择整个排序范围内的第一个元素作为基准，对排序范围内的元素进行快速排序
- 先从最右边向左看，依次将每一个元素与基准元素进行比较，如果发现比基准元素小，那么就与左边遍历位置上的元素（一开始是基准元素的位置）进行交换，此时保留右边当前遍历的位置。
- 交换后，转为从左往右开始遍历元素，如果发现比基准元素大，那么就与之前保留的右边遍历的位置上的元素进行交换，同样保留左边当前的位置，循环执行上一个步骤。
- 当左右遍历撞到一起时，本轮快速排序完成，最后在最中间的位置就是基准元素的位置了。
- 以基准位置为中心，划分左右两边，以同样的方式执行快速排序。
#### 双轴快速排序
在极端情况下，快速排序会退化为冒泡排序
[原始论文](https://codeblab.com/wp-content/uploads/2009/09/DualPivotQuicksort.pdf)
![](https://pic3.zhimg.com/v2-f51be004788c2996d464e8a5fa279874_1440w.jpg)
1. 对于小序列，长度小于27的序列，使用插入排序。（JDK 1.8中使用的是47）
2. 选择两个轴元素P1、P2。例如图中选择第一个元素`a[left] = P1`，最后一个元素`a[right] = P2`。
3. 我们限定P1 < P2（如果不是，则交换P1、P2）。这样我们就可以分出如下部分：Part I `[left, L - 1]`为< P1部分，Part II `[L, K - 1]`为 >= P1且<= P2部分，Part III `[G + 1, right - 1]`为> P2部分，Part IV `[K, G]`为还没有确定的部分。
4. 针对当前Part IV中的`a[K]`，与P1和P2比较，比较后放入Part I II III中的一个。
5. 调整L、K、G到适合的位置。
6. 重复4、5步直到K > G。也就是说PartIV的元素全部分散到Part I II III中，最后是三个Part。
7. 将P1放入PartI的最后一个位置，P2放入PartIII的前一个位置（或者第一个位置）。这样就确定了P1和P2的位置。
8. 对于PartI II III ，重复1-7步。
实验表明在随机分布的序列中，需要排序的序列长度越大，双轴快排的性能领先越明显。
The supposed algorithm has additional improvement of the special choice procedure for pivot elements P1 and P2. We take not the first a[left] and last a[right] elements of the array, but choose two middle elements
### 希尔排序
希尔排序是直接**插入排序**的进阶版本（希尔排序又叫**缩小增量排序**）
会对整个数组按照步长进行分组，优先比较距离较远的元素。
大量研究表明，当增量序列为 `dlta[k] = 2^(t-k+1)-1（0<=k<=t<=(log2(n+1))）`时，效率很好，只不过为了简单，我们一般使用$\frac{​n}{2}、\frac{n}{4}​、\frac{n​}{8}、...、1$这样的增量序列。

原理是每一组的第一个元素天然是有序的，然后从第二个元素开始进行插入排序；相当于是把长度n的数组分为长度为n/gap的gap组，每组分别进行插入排序，排序后gap//2，再进行排序

- 首先求出最初的步长，n/2即可。
- 我们将整个数组按照步长进行分组，也就是两两一组（如果n为奇数的话，第一组会有三个元素）
- 我们分别在这些分组内进行插入排序。
- 排序完成后，我们将步长/2，重新分组，重复上述步骤，直到步长为1时，插入排序最后一遍结束。

希尔排序是**不稳定的**排序算法
### 堆排序
用数组表示堆
先建堆，后排序（循环将最顶上的元素排到最后）
不稳定

| 排序算法 | 最好情况     | 最坏情况     | 空间复杂度   | 稳定性 |
| ---- | -------- | -------- | ------- | --- |
| 快速排序 | O(nlogn) | O(n^2)   | O(logn) | 不稳定 |
| 希尔排序 | O(n^1.3) | O(n^2)   | O(1)    | 不稳定 |
| 堆排序  | O(nlogn) | O(nlogn) | O(1)    | 不稳定 |

## 其他排序算法
### 归并排序
归并排序利用递归分治的思想，将原本的数组进行划分，然后首先对划分出来的小数组进行排序，然后最后在合并为一个有序的大数组
是稳定的排序算法
### 桶排序
数据必须是**均匀分布**的。
知道最小值和最大值
### 基数排序
只建 10 个桶（0 到 9）
按个位、十位、百位...的顺序丢进桶里并按顺序拿出来
**LSD（Least Significant Digit，从最低位开始）** 基数排序
**必须保证是稳定排序（Stable）**
**倒序遍历**，保证稳定性

| 排序算法 | 最好情况     | 最坏情况     | 空间复杂度   | 稳定性 |
| ---- | -------- | -------- | ------- | --- |
| 冒泡排序 | O(n)     | O(n2)    | O(1)    | 稳定  |
| 插入排序 | O(n)     | O(n2)    | O(1)    | 稳定  |
| 选择排序 | O(n2)    | O(n2)    | O(1)    | 不稳定 |
| 快速排序 | O(nlogn) | O(n2)    | O(logn) | 不稳定 |
| 希尔排序 | O(n1.3)  | O(n2)    | O(1)    | 不稳定 |
| 堆排序  | O(nlogn) | O(nlogn) | O(1)    | 不稳定 |
| 归并排序 | O(nlogn) | O(nlogn) | O(n)    | 稳定  |
| 计数排序 | O(n+k)   | O(n+k)   | O(k)    | 稳定  |
| 桶排序  | O(n+k)   | O(n2)    | O(k+n)  | 稳定  |
| 基数排序 | O(n×k)   | O(n×k)   | O(k+n)  | 稳定  |
