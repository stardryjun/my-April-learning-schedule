# 数组
**数组是存放在连续内存空间上的相同类型数据的集合。**
## 二分查找
- [35.搜索插入位置](https://programmercarl.com/0035.%E6%90%9C%E7%B4%A2%E6%8F%92%E5%85%A5%E4%BD%8D%E7%BD%AE.html)
- [34.在排序数组中查找元素的第一个和最后一个位置](https://programmercarl.com/0034.%E5%9C%A8%E6%8E%92%E5%BA%8F%E6%95%B0%E7%BB%84%E4%B8%AD%E6%9F%A5%E6%89%BE%E5%85%83%E7%B4%A0%E7%9A%84%E7%AC%AC%E4%B8%80%E4%B8%AA%E5%92%8C%E6%9C%80%E5%90%8E%E4%B8%80%E4%B8%AA%E4%BD%8D%E7%BD%AE.html)
- [69.x 的平方根(opens new window)](https://leetcode.cn/problems/sqrtx/)
- [367.有效的完全平方数(opens new window)](https://leetcode.cn/problems/valid-perfect-square/)
	乘法会溢出，可以使用除法或者奇数相加等于完全平方数来计算
### 第一种写法
`[left,right]`
- while (left <= right) 要使用 <= ，因为left == right是有意义的，所以使用 <=
- if (nums[middle] > target) right 要赋值为 middle - 1，因为当前这个nums[middle]一定不是target，那么接下来要查找的左区间结束下标位置就是 middle - 1
### 第二种写法
`[left,right)`
- while (left < right)，这里使用 < ,因为left == right在区间[left, right)是没有意义的
- if (nums[middle] > target) right 更新为 middle，因为当前nums[middle]不等于target，去左区间继续寻找，而寻找区间是左闭右开区间，所以right更新为middle，即：下一个查询区间不会去比较nums[middle]

要在二分查找的过程中，保持不变量
## 移除元素
- [26.删除排序数组中的重复项](https://leetcode.cn/problems/remove-duplicates-from-sorted-array/)
- [283.移动零(opens new window)](https://leetcode.cn/problems/move-zeroes/)
- [844.比较含退格的字符串](https://leetcode.cn/problems/backspace-string-compare/)
- [977.有序数组的平方](https://leetcode.cn/problems/squares-of-a-sorted-array/)
### 快慢指针法
使用快慢指针最大的好处就是：**能在不额外增加空间复杂度（保持 O(1) 空间）的前提下，将时间复杂度降到最低，通常只需遍历一次（O(N)）。**
#### 场景一：判断链表是否有环
#### 场景二：寻找链表的中点
快指针走 2 步，慢指针走 1 步。
当快指针走到链表末尾时，慢指针刚好走到一半
#### 场景三：寻找链表的倒数第 K 个节点
#### 场景四：数组的原地修改（如：删除有序数组中的重复项）
只要**快指针发现**当前元素和慢指针的元素不一样，就把快指针的值抄到慢指针的下一个位置。

### 滑动窗口
[长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)
使用外部变量来记录内部状态，不用每一次都去循环检查内部状态
- [904.水果成篮(opens new window)](https://leetcode.cn/problems/fruit-into-baskets/)
	`collections.defaultdict(int)`
	左闭右闭区间`fast-slow+1`，左闭右开区间`fast-slow`
- [76.最小覆盖子串(opens new window)](https://leetcode.cn/problems/minimum-window-substring/)

## 螺旋矩阵
[螺旋矩阵II](https://leetcode.cn/problems/spiral-matrix-ii/)
**循环不变量原则**
- [54.螺旋矩阵(opens new window)](https://leetcode.cn/problems/spiral-matrix/)

## 前缀和
[区间和](https://kamacoder.com/problempage.php?pid=1070)
```
try:
except EOFError:
	pass
```
`input = sys.stdin.read`
`data = input().split()`
[开发商购买土地](https://kamacoder.com/problempage.php?pid=1044)
`[[0]*m for i in range(n)]`

# 链表
定义构造函数
C++默认生成的构造函数**不会初始化**任何成员变量

循环链表可以用来解决约瑟夫环问题。

## 移除链表元素
可以使用原链表来直接进行删除操作，也可以设置一个**虚拟头结点**再进行删除操作
[移除链表元素](https://leetcode.cn/problems/remove-linked-list-elements/)
**可以通过递归的思路解决本题**
[设计链表](https://leetcode.cn/problems/design-linked-list/)

## 反转链表
双指针法
递归法
```
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        // 边缘条件判断
        if(head == NULL) return NULL;
        if (head->next == NULL) return head;
        
        // 递归调用，翻转第二个节点开始往后的链表
        ListNode *last = reverseList(head->next);
        // 翻转头节点与第二个节点的指向
        head->next->next = head;
        // 此时的 head 节点为尾节点，next 需要指向 NULL
        head->next = NULL;
        return last;
    }
}; 
```
使用虚拟头结点解决链表反转
使用栈解决反转链表的问题

[两两交换链表中的节点](https://leetcode.cn/problems/swap-nodes-in-pairs/)

[删除链表的倒数第N个节点](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/)
一次遍历的方法：双指针法

[链表相交](https://leetcode.cn/problems/intersection-of-two-linked-lists-lcci/)
双指针
逻辑互斥，用if else

[环形链表II](https://leetcode.cn/problems/linked-list-cycle-ii/)
环形链表fast和slow相遇时，相遇点回到环入口的距离和从出发点到环入口的距离相同
**Floyd 判圈算法**

# 哈希表
哈希表/散列表
**一般哈希表都是用来快速判断一个元素是否出现集合里**
## 哈希碰撞
### 拉链法
### 线性探测法
一定要保证tableSize大于dataSize
## 常见的哈希结构
在c++中，

|集合|底层实现|是否有序|数值是否可以重复|能否更改数值|查询效率|增删效率|
|---|---|---|---|---|---|---|
|std::set|红黑树|有序|否|否|O(log n)|O(log n)|
|std::multiset|红黑树|有序|是|否|O(logn)|O(logn)|
|std::unordered_set|哈希表|无序|否|否|O(1)|O(1)|
红黑树是一种平衡二叉搜索树，所以key值是有序的，但key**不可以修改**，改动key值会导致整棵树的错乱，所以只能删除和增加。

|映射|底层实现|是否有序|数值是否可以重复|能否更改数值|查询效率|增删效率|
|---|---|---|---|---|---|---|
|std::map|红黑树|key有序|key不可重复|key不可修改|O(logn)|O(logn)|
|std::multimap|红黑树|key有序|key可重复|key不可修改|O(log n)|O(log n)|
|std::unordered_map|哈希表|key无序|key不可重复|key不可修改|O(1)|O(1)|

## 有效的字母异位词
[有效的字母异位词](https://leetcode.cn/problems/valid-anagram/)
英文字母范围**用数组**代替哈希表，节省开销；不需要两个表，可以对一个表进行加减操作
- [383.赎金信](https://programmercarl.com/0383.%E8%B5%8E%E9%87%91%E4%BF%A1.html)
- [49.字母异位词分组](https://leetcode.cn/problems/group-anagrams/)
- [438.找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/)
	滑动窗口思想，不需要重复计算，只需要在窗口滑动的时候添加或者减少元素即可
	当左右两个边界都包含的时候，长度为right-left+1

## 两个数组的交集
[两个数组的交集](https://leetcode.cn/problems/intersection-of-two-arrays/)
	数据量不大的情况下，对于字母使用数组而不是unordered_map/unordered_set
- [350.两个数组的交集 II(opens new window)](https://leetcode.cn/problems/intersection-of-two-arrays-ii/)

## 快乐数
[快乐数](https://leetcode.cn/problems/happy-number/)
`set.find(sum) != set.end()`

## 两数之和
**什么时候使用哈希法**：当我们需要查询一个元素是否出现过，或者一个元素是否在集合里的时候，就要第一时间想到哈希法。

[四数相加II](https://leetcode.cn/problems/4sum-ii/)
[三数之和](https://leetcode.cn/problems/3sum/)
	哈希法难去重，使用双指针法
	all()
	Counter() - Counter()
[四数之和](https://leetcode.cn/problems/4sum/)
	long long类型要在=号后也强制转换，否则是先计算为int再转换为long long

## 总结
一些应用场景就是为数组量身定做的。

# 字符串
## 反转字符串
[反转字符串](https://leetcode.cn/problems/reverse-string/)
	C++交换：swap（）
	通过位运算交换（异或）（不推荐使用，因为和自己交换会被清零）
	python对原字符串进行操作：`s[:]=`,否则不会改变原值，只会改变s的指向
[反转字符串II](https://leetcode.cn/problems/reverse-string-ii/)
	看题解

[替换数字](https://kamacoder.com/problempage.php?pid=1064)
**很多数组填充类的问题，其做法都是先预先给数组扩容带填充后的大小，然后在从后向前进行操作。**

- [4. 翻转字符串里的单词](https://programmercarl.com/0151.%E7%BF%BB%E8%BD%AC%E5%AD%97%E7%AC%A6%E4%B8%B2%E9%87%8C%E7%9A%84%E5%8D%95%E8%AF%8D.html)
	python，java中字符串不可变，c++中可变
	使用双端对列的数据结构代替reverse
	依旧可以使用双指针
	使用C++自带istringstream
	整体反转+局部反转

[右旋字符串](https://kamacoder.com/problempage.php?pid=1065)
	整体反转+局部反转
	python中index()会抛出异常，find()会返回-1；
## KMP
[28. 找出字符串中第一个匹配项的下标 - 力扣（LeetCode）](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/)
	KMP：O(m+n)
	最长公共前后缀：在匹配过程中对于被搜索的字符串只向前移动，只回退目标字符串
	递推求解next数组
	前缀表：**记录下标i之前（包括i）的字符串中，有多大长度的相同前缀后缀。**
[重复的子字符串](https://leetcode.cn/problems/repeated-substring-pattern/)

## 总结
### 双指针法
**双指针法在数组，链表和字符串中很常用。**
**很多数组填充类的问题，都可以先预先给数组扩容带填充后的大小，然后在从后向前进行操作。**
### 反转
**当需要固定规律一段一段去处理字符串的时候，要想想在在for循环的表达式上做做文章**。
**先整体反转再局部反转**
**先局部反转再整体反转**
### KMP
**针对前缀表到底要不要减一，这其实是不同KMP实现的方式**
**理解j=next[x]**

# 栈与对列
**栈是以底层容器完成其所有的工作，对外提供统一的接口，底层容器是可插拔的（也就是说我们可以控制使用哪种容器来实现栈的功能）。**
所以STL中栈往往不被归类为容器，而被归类为container adapter（容器适配器）。

**我们常用的SGI STL，如果没有指定底层实现的话，默认是以deque为缺省情况下栈的底层结构。**
**SGI STL中 队列底层实现缺省情况下一样使用deque实现的。**
所以STL 队列也不被归类为容器，而被归类为container adapter（ 容器适配器）。

## 用栈实现队列
[用栈实现队列](https://leetcode.cn/problems/implement-queue-using-stacks/)
代码复用，抽象为函数

